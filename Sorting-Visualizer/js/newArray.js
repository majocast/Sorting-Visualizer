function newArray(param) {
  int parameter = param;
  for(int i = 0; i<=parameter; i++) {
    var length = randomInt(100);
  }
}

function randomInt(max) {
  return Math.floor(Math.random() * max);
}
